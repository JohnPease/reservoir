{"id":"reservoir-1et","title":"Verify currentBalance formula sign for goal progress (sums attributed spend transactions additively)","description":"Engineer flagged during reservoir-adq.5 implementation: GoalsScreenCalculator.currentBalance implements the bead's literal spec: currentBalance = startingBalance + sum(all SpendTransactions attributed to this goal). Since SpendTransaction.amount is always a positive spend magnitude and there is no 'deposit'/contribution transaction kind in this app, this means MORE spending on a goal's attributed transactions moves the progress bar UP toward targetAmount — the opposite of the app's core 'underspend banks savings, carry-forward' mechanic used everywhere else (daily limit, Today screen). Implemented exactly as the JP-approved bead spec states (with a code comment flagging it), not silently changed, but worth a second look before this ships: does 'attributed transaction' for goal progress mean something different from the spending SpendTransaction used in daily-limit math (e.g. a future separate contribution-tracking concept), or is the formula sign actually backwards and should be currentBalance = startingBalance + banked-surplus-style accounting instead? Needs JP/product-lead decision before this ships to avoid a confusing progress bar that moves the wrong direction.","notes":"JP decision (2026-07-10), confirmed with precise walkthrough: goal progress = startingBalance + carryForward (banked-surplus model), matching the app's core daily-limit mechanic exactly. Confirmed operational semantics: spending $0/day banks the full dailyBase toward the goal each day (100% of target reached by targetDate if never spent); spending exactly today's displayed daily limit (dailyBase + carryForward) nets ZERO progress that day (neutral/on-pace but no gain); spending over the limit erodes progress (carryForward goes negative). JP explicitly confirmed this is the intended model ('100% that's exactly what I was thinking'). Implementation: GoalsScreenCalculator.currentBalance should become startingBalance + carryForward (via DailyLimitCalculator/GoalCarryForwardInput, as of referenceDate) — replacing the current startingBalance + sum(attributed transactions) formula. Ready for implementation.","status":"closed","priority":1,"issue_type":"bug","owner":"johnjpease@gmail.com","created_at":"2026-07-09T02:25:28Z","created_by":"johnpease","updated_at":"2026-07-11T02:59:56Z","closed_at":"2026-07-11T02:59:56Z","close_reason":"Fixed in commit ab872f0 (feature/goals-screen, PR #5): currentBalance now computed as startingBalance + carryForward, matching JP's confirmed banked-surplus semantics. Verified personally (not via relay): 119/119 unit tests pass including 5 new tests exercising the exact confirmed walkthrough (zero-spend reaches targetAmount exactly, spending exactly the daily limit nets zero change, overspending erodes below startingBalance, fixed transactions excluded). 2 UI test failures are the pre-existing unrelated reservoir-4w8 flake.","dependencies":[{"issue_id":"reservoir-1et","depends_on_id":"reservoir-adq.5","type":"discovered-from","created_at":"2026-07-08T21:25:28Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"reservoir-adq.6","title":"Plaid Link integration + transaction import","description":"Integrate Plaid iOS LinkKit per docs/PROJECT_SPEC.md: access_token stored in Keychain (never UserDefaults/committed config); Sandbox during dev, Production once linked to real accounts via build config; no webhooks — refresh on app foreground + manual pull-to-refresh. Dedup: before import, check existing manual transactions with same date+amount(±same-day)+merchant, prompt to merge rather than double-count (this is an MVP requirement, not deferred). Highest external risk — build last per recommended build order, after core logic is de-risked.","status":"open","priority":1,"issue_type":"feature","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:54Z","created_by":"johnpease","updated_at":"2026-07-07T05:10:54Z","dependencies":[{"issue_id":"reservoir-adq.6","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.6","depends_on_id":"reservoir-adq.3","type":"blocks","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"reservoir-adq.2","title":"Today screen (SwiftUI)","description":"Implement the Today tab per docs/PROJECT_SPEC.md UX section: date header with settings icon, hero daily-limit number (44px, centered) with '$base + $carried forward' subtext (single combined sum across all active goals, not per-goal breakdown), two-stat row (spent today / remaining, summed across active goals), recent transactions (last 3, sorted by date then creation order; fixed ones muted 'excluded from limit'), single 'Add transaction' primary action (renders as a minimal stub sheet for now — full entry flow is adq.3). Wire to real SwiftData-backed calculator output via a small pure/testable mapping function (SavingsGoal/SpendTransaction -\u003e GoalCarryForwardInput), not inlined in the view.\n\nGoal lifecycle: a goal is \"active\" when targetDate \u003e= today. When a goal's targetDate passes, show a success banner on Today with an encouraging completion message. Dismissing the banner resets the goal to a true empty/no-goal state (goal is cleared; user must create a new one to see the hero number again) — requires a persisted dismissal field on SavingsGoal (e.g. dismissedAt) since this can't be derived from targetDate alone.\n\nEmpty states: no active goal -\u003e prompt to create a goal instead of hero number (stub nav/sheet for now — full creation flow is adq.5/adq.7); no transactions -\u003e empty-state message, $0 spent / full limit remaining; over-limit -\u003e remaining renders as negative/clearly-over state. Handle orphaned transactions (savingsGoal == nil) explicitly in the spent/remaining sums. Recompute on midnight rollover and on foreground resume. Assume USD formatting.\n\n44px hero sizing is a manual/code-review check (STANDARDS §5), not XCUITest-assertable. Cover presence/content/state transitions (empty state, banner, dismiss-and-reset, normal state) with XCUITest per STANDARDS.md §5.","status":"closed","priority":1,"issue_type":"feature","assignee":"johnpease","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:38Z","created_by":"johnpease","updated_at":"2026-07-08T03:15:28Z","closed_at":"2026-07-08T03:15:28Z","close_reason":"Merged via PR #3 (https://github.com/JohnPease/reservoir/pull/3) after all 8 code review findings fixed on feature/today-screen (commit 14579ec).","dependencies":[{"issue_id":"reservoir-adq.2","depends_on_id":"reservoir-2bd","type":"blocks","created_at":"2026-07-07T00:10:38Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.2","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:38Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":1}
{"id":"reservoir-2bd","title":"Daily limit / carry-forward calculator","description":"Standalone, unit-tested pure Swift function (no SwiftUI/SwiftData import) implementing the core mechanic from docs/PROJECT_SPEC.md: base = (targetAmount - startingBalance) / totalDaysFromStart, fixed at goal creation/edit; continuous carry-forward (overspend borrows from future days, underspend adds); day boundary = device local midnight; when multiple SavingsGoals are active, total daily limit = sum of each goal's independent base + carry-forward (not pooled); fixed-tagged transactions reduce goal balance but are excluded from the limit/carry-forward math. Cover in unit tests: overspend day, underspend day, goal edited mid-stream, multi-day gaps, multiple concurrent goals, fixed vs variable transaction handling. Target \u003e=80% coverage per STANDARDS.md §5.","status":"closed","priority":1,"issue_type":"feature","assignee":"johnpease","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:03Z","created_by":"johnpease","updated_at":"2026-07-08T01:13:00Z","closed_at":"2026-07-08T01:13:00Z","close_reason":"Implemented pure-Swift DailyLimitCalculator + 14 XCTest cases; verified 19/19 tests pass, 98.33% coverage on the new file. Committed as 5f2c58b on feature/daily-limit-carry-forward, pushed to origin.","dependencies":[{"issue_id":"reservoir-2bd","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:21Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-2bd","depends_on_id":"reservoir-adq.1","type":"blocks","created_at":"2026-07-07T00:10:02Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"id":"reservoir-adq.1","title":"SwiftData models: SavingsGoal, Transaction, MerchantRule","description":"Define SwiftData @Model types per docs/PROJECT_SPEC.md data model section: SavingsGoal (targetAmount, targetDate, startDate, startingBalance, derived currentBalance, derived dailyBase fixed at creation/edit), Transaction (amount, date, merchantName, type variable/fixed, entryMethod manual/imported, plaidTransactionID), MerchantRule (merchantName exact case-insensitive match, type). Wrap in a VersionedSchema (SchemaV1) from the start per STANDARDS.md §4. No User entity — settings live in AppSettings/UserDefaults.","status":"closed","priority":1,"issue_type":"feature","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:09:45Z","created_by":"johnpease","updated_at":"2026-07-07T06:12:54Z","closed_at":"2026-07-07T06:12:54Z","close_reason":"Merged via PR #1 into develop; SwiftData models, VersionedSchema, and review fixes landed","dependencies":[{"issue_id":"reservoir-adq.1","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:09:44Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"id":"reservoir-adq","title":"Daily Limit Finance App — MVP","description":"Personal iOS app tracking spending against a rolling daily limit derived from savings goals. See docs/PROJECT_SPEC.md for full spec. This epic tracks the MVP build order: data models -\u003e calculator -\u003e Today screen -\u003e transactions/goals screens -\u003e Plaid integration.","status":"open","priority":1,"issue_type":"epic","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:09:28Z","created_by":"johnpease","updated_at":"2026-07-07T05:09:28Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"reservoir-4za","title":"Verify goal-met (not just target-date-passed) before showing success banner copy","description":"Code review on PR #3 (reservoir-adq.2 Today screen) flagged that the completion banner's success framing was asserted purely from targetDate having passed, with no check that the goal's remaining balance was actually \u003e= 0 across its lifetime. Fixed for now by softening the banner copy to 'Your goal's target date has arrived' instead of 'You reached your target... Nice work' (see TodayView.CompletionBannerView). This story replaces that stopgap with real goal-met verification.\n\n## 1. Definition of \"goal met\"\n\n\"Goal met\" = **end-state check only**, not a running/day-by-day check. Specifically: the goal's cumulative carry-forward balance as of the day after targetDate (i.e. summed over every day from `effectiveStartDate` through `targetDate` inclusive) is \u003e= 0.\n\nWhy end-state and not running: carry-forward is the app's core mechanic, and its entire purpose is to let a bad day borrow from future days and get paid back — that's not a failure state, it's the mechanic working as designed. A goal where the user overspent on day 3 and recovered by day 20 is indistinguishable, in outcome, from a goal where every day was exactly on-budget. Disqualifying \"met\" status because of a single over-limit day anywhere in the lifetime would contradict the forgiving-by-design nature of carry-forward and would read as punitive/wrong to JP. Only the final position — did the money actually last the full plan — determines success. One over-limit day that gets recovered does NOT disqualify the goal from \"met.\"  Only the cumulative position at the end does.\n\nThis mirrors `DailyLimitCalculator.carryForward`'s own math almost exactly: `carryForward(for:asOf:)` already sums `dailyBase - spent` over every day from `effectiveStartDate` up to (excluding) `referenceDate`'s day. Calling it with `referenceDate = calendar.date(byAdding: .day, value: 1, to: targetDate)` sums through targetDate inclusive and gives exactly the lifetime cumulative surplus needed — entries dated after targetDate are naturally excluded since the day-loop only visits offsets within that window. No changes to `carryForward` itself are needed; this is a new thin wrapper, not new day-by-day logic.\n\nNote this reuses `effectiveStartDate` (resets on goal edit), consistent with how carry-forward already treats an edit as a new goal state — no special-casing needed for edited goals.\n\n## 2. Technical approach\n\nAdd to `DailyLimitCalculator` (pure, testable, no SwiftData):\n```swift\n/// Whether the goal's cumulative carry-forward balance was \u003e= 0 across its full\n/// lifetime (effectiveStartDate through targetDate inclusive) — i.e. the user never\n/// net-overspent once the whole plan is taken into account. A day where the user went\n/// over their daily limit but recovered later does NOT disqualify — only the final\n/// cumulative position at targetDate matters (carry-forward is designed to absorb\n/// exactly this kind of fluctuation).\npublic static func isGoalMet(\n    for goal: GoalCarryForwardInput,\n    targetDate: Date,\n    calendar: Calendar = .current\n) -\u003e Bool {\n    let dayAfterTarget = calendar.date(byAdding: .day, value: 1, to: targetDate)!\n    return carryForward(for: goal, asOf: dayAfterTarget, calendar: calendar) \u003e= 0\n}\n```\n\nAdd to `TodayScreenCalculator` a thin SwiftData-aware wrapper (mirrors the existing `carryForwardInput(for:)` pattern):\n```swift\nstatic func isGoalMet(_ goal: SavingsGoal, calendar: Calendar = .current) -\u003e Bool {\n    DailyLimitCalculator.isGoalMet(\n        for: carryForwardInput(for: goal),\n        targetDate: goal.targetDate,\n        calendar: calendar\n    )\n}\n```\n\n`TodayView.CompletionBannerView` (or its caller iterating `completedUndismissedGoals`) calls `TodayScreenCalculator.isGoalMet(goal)` per goal to select copy — `goal.transactions` is already available via the existing relationship, no new `@Query` needed. `TodayScreenCalculator.summary()` does NOT need a new field — this is a per-goal completion-banner concern, not part of the active-goal summary struct.\n\n## 3. UX: two distinct banner states (not one softened message)\n\n**Met** (isGoalMet == true):\n- Headline: \"You reached your goal — nice work!\"\n- Tone: celebratory, matches the app's existing positive-number-green styling elsewhere.\n\n**Not met** (isGoalMet == false):\n- Headline: \"Your target date has arrived\"\n- Subtext: \"You spent more than planned along the way.\" — factual, not shaming; no guilt language (\"you failed\", \"you went over budget by a lot\"), consistent with product-lead's mandate not to be punitive.\n- Do NOT show the numeric shortfall amount in the banner in this pass — keep the banner text-only per the existing CompletionBannerView shape. (A future story could add the shortfall figure if JP wants it; explicitly out of scope here to avoid scope creep on a copy-accuracy fix.)\n\nBoth states are dismissible the same way as today (existing dismiss behavior unchanged).\n\n## Out of scope\n- Any change to `carryForward`'s day-by-day loop or to how active-goal `remaining` is computed/displayed on the Today screen.\n- Showing the numeric overspend/shortfall amount in the banner.\n- Any \"running\" or mid-lifetime warning about pace (that's a distinct, larger feature — daily-limit-carry-forward epic already covers in-progress pacing elsewhere).\n- Historical/completed-goals list view changes — this only affects the Today screen's completion banner.","acceptance_criteria":"Unit tests (DailyLimitCalculatorTests, add to existing file):\n- isGoalMet == true when spend was under dailyBase every day (clean underspend).\n- isGoalMet == true when one or more days went over dailyBase but the goal recovered by targetDate (cumulative \u003e= 0 at end) — proves a bad day alone does not disqualify.\n- isGoalMet == false when cumulative carry-forward at targetDate is negative, even if the most recent day(s) were under budget (proves it's the end-state, not the last day, that decides).\n- isGoalMet == false when the user overspent only on the final day, tipping a previously-positive cumulative balance negative.\n- isGoalMet with no spend entries at all (edge case: dailyBase * totalDays, i.e. treated as full underspend) returns true.\n- Boundary case: cumulative balance exactly 0 at targetDate returns true (\"\u003e= 0\", not \"\u003e 0\").\n\nUnit tests (TodayScreenCalculatorTests, add to existing file):\n- isGoalMet(_:) wrapper correctly maps a SavingsGoal + its transactions through to the same result as the DailyLimitCalculator test above, for at least one met and one not-met fixture.\n\nManual/UI check (TodayView):\n- Construct a completed-undismissed goal that nets positive lifetime carry-forward; confirm the \"You reached your goal — nice work!\" banner renders.\n- Construct a completed-undismissed goal that nets negative lifetime carry-forward; confirm the \"Your target date has arrived\" / \"You spent more than planned along the way.\" banner renders instead, with neutral (not red/alarm) styling — this is a past event being reported factually, not an active warning state.\n- Confirm dismiss behavior is unchanged for both variants.\n\nDefinition of done: both DailyLimitCalculator.isGoalMet and TodayScreenCalculator.isGoalMet exist and are covered by the tests above; TodayView.CompletionBannerView shows the two distinct copy variants keyed off isGoalMet instead of the current single softened message; no change to active-goal remaining/limit display.","notes":"FLAG FOR JP — please confirm before engineer builds:\n\n1. Priority: currently P2. This is cosmetic (banner copy accuracy), not functional — it doesn't affect the daily limit, carry-forward math, or any number the user relies on day-to-day. It only affects what a dismissible one-time completion banner says after a goal's target date passes. I think P2 is defensible but this could reasonably drop to P3 if the backlog is under pressure; it's not blocking anything. Your call.\n\n2. Definition of \"goal met\" — this is the part that actually matters for correctness perception: I landed on an END-STATE check (cumulative carry-forward balance \u003e= 0 at targetDate), NOT a \"never had a single over-limit day\" check. Reasoning: carry-forward exists specifically so a bad day can be paid back later — that's the mechanic working as intended, not a failure. If we disqualified \"met\" status the moment any single day went over, that would contradict the whole premise of carry-forward and would likely feel wrong/punitive to you when you check the app. Please confirm this matches your mental model of \"did I hit my goal\" before this gets built — if you actually want a stricter \"never went over, even for a day\" definition, that's a different (and larger) calculation and changes the story.","status":"closed","priority":2,"issue_type":"task","assignee":"johnpease","owner":"johnjpease@gmail.com","created_at":"2026-07-08T03:10:24Z","created_by":"johnpease","updated_at":"2026-07-09T01:05:58Z","closed_at":"2026-07-09T01:05:58Z","close_reason":"Merged via PR #4 (https://github.com/JohnPease/reservoir/pull/4). Goal-met verification implemented: DailyLimitCalculator.isGoalMet (end-state cumulative carry-forward check) + TodayScreenCalculator.isGoalMet wrapper + two-state CompletionBannerView copy. 63/63 tests passing, 100% coverage on new functions. Code-reviewed clean.","dependencies":[{"issue_id":"reservoir-4za","depends_on_id":"reservoir-adq.2","type":"discovered-from","created_at":"2026-07-07T22:10:23Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"id":"reservoir-adq.7","title":"Settings screen (linked accounts, balances, goal management)","description":"Settings tab: linked accounts management (Plaid Link entry point, relink/unlink), starting balances entry (onboarding also uses this), goal management (create/edit SavingsGoal, list active goals for multi-goal support).","notes":"Flagged during reservoir-adq.5's scoping/sanity pass (2026-07-08): this bead's description still lists 'goal management (create/edit SavingsGoal, list active goals)' as in scope, but adq.5 now owns full goal CRUD (create/edit/delete/list) directly on the Goals tab. Since adq.7 depends on adq.5, the likely intent is Settings should link into adq.5's flows rather than reimplement them — but this bead's description hasn't been updated to reflect that. Needs a scope pass before adq.7 is picked up, not resolved here.","status":"open","priority":2,"issue_type":"feature","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:11:01Z","created_by":"johnpease","updated_at":"2026-07-09T01:22:25Z","dependencies":[{"issue_id":"reservoir-adq.7","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:11:01Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.7","depends_on_id":"reservoir-adq.5","type":"blocks","created_at":"2026-07-07T00:11:01Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.7","depends_on_id":"reservoir-adq.6","type":"blocks","created_at":"2026-07-07T00:11:01Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"id":"reservoir-adq.4","title":"MerchantRule auto-tagging","description":"**Narrowed scope (2026-07-07 reconciliation):** originally scoped to cover all MerchantRule auto-tagging, including retroactive re-application to existing transactions. JP has since decided retroactive re-tagging happens synchronously whenever a MerchantRule is created or edited — that behavior, and the merchant-match engine it depends on, is now built as part of reservoir-adq.3 (see that bead's \"Scope note\" and acceptance criteria), since adq.3's own MerchantRule CRUD UI needs it immediately and it would be wasteful to build it twice.\n\n**What this story owns now**: applying the same merchant-match function at *import time* — when Plaid delivers new transactions (depends on the not-yet-built Plaid integration, reservoir-adq.6), auto-tag each newly-imported `SpendTransaction` using the existing `[MerchantRule]` set before it's saved/shown to the user. A per-transaction manual tag always overrides a MerchantRule match (not applicable to freshly-imported transactions, which start with `isManualOverride == false`, but relevant if a rule later changes and adq.3's retag pass runs against them). No fuzzy/substring matching in MVP — exact, case-insensitive merchant name match only, matching adq.3's semantics.\n\n**Do not rebuild the matching engine.** adq.3 builds and exposes a pure `Services/` function (`[MerchantRule] -\u003e String -\u003e TransactionType?`) for exactly this lookup — this story calls that function at import time rather than re-deriving matching logic. If adq.3's function isn't structured for reuse from this feature area when this story starts, that's a defect in adq.3, not a reason to duplicate it here — flag back to product-lead rather than reimplementing.\n\nDepends on reservoir-adq.3 (for the shared matching function) and, for actual import-time invocation, on reservoir-adq.6 (Plaid Link integration) — this story's UI/data surface is otherwise ready once adq.3 lands, but it can't be meaningfully exercised end-to-end until adq.6 exists. Engineer/product-lead judgment call at planning time on whether to sequence this strictly after adq.6 or build the import-time hook in parallel against a stub import source.\n","status":"open","priority":2,"issue_type":"feature","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:54Z","created_by":"johnpease","updated_at":"2026-07-08T03:57:01Z","dependencies":[{"issue_id":"reservoir-adq.4","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.4","depends_on_id":"reservoir-adq.3","type":"blocks","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"id":"reservoir-adq.5","title":"Goals screen (progress + pace projection)","description":"Goals tab: progress and \"at current pace\" projections per SavingsGoal, derived from the calculator's dailyBase/carry-forward output. Goal creation/edit flow enforces validation: targetDate after startDate and after today at creation time; startingBalance \u003e= 0. All five open product questions from the original spec are now decided (see comments) and folded in below.\n\n## Screen contents\n\nGoals tab shows three sections, in this order, each collapsible/hidden when empty:\n1. **Active goals** (isActive per TodayScreenCalculator — targetDate \u003e= today, dismissedAt == nil). Sorted by targetDate ascending (soonest first), matching the Today screen's @Query sort — one sort convention across the app.\n2. **Completed, awaiting dismissal** (completedUndismissedGoals — same set that shows a Today-screen banner). Rendered with the same \"reached its target date\" framing as Today's CompletionBannerView, plus a dismiss action that does the same dismissedAt = .now write TodayView.dismiss(_:) already does — reuse that logic (extract to TodayScreenCalculator or a shared spot, don't duplicate the SwiftData save/rollback pattern per STANDARDS §3).\n3. **Dismissed goals**: vanish entirely — CONFIRMED (JP decision #4). Not shown anywhere in this bead; no history/archive view. Matches Today's dismiss flow treating dismissal as terminal.\n\nEmpty state: if there are zero goals of any kind, show the same \"No active goal yet / Create a savings goal\" prompt pattern as Today's NoActiveGoalPrompt (reuse the copy/spirit, this is a second entry point to the same empty state — do not invent different copy).\n\n**Multi-goal display: one card per goal — CONFIRMED (JP decision #1).** Each active goal renders as its own independent card with its own progress/pace. No consolidated/combined view.\n\n### Per-goal card, active section\n\n- **Progress bar + percentage**: `(currentBalance - startingBalance) / (targetAmount - startingBalance)`, clamped to [0, 1] for the bar fill (can display negative/over-100% as text even if the bar itself clamps). `currentBalance` for a goal without a linked account (pre-Plaid, MVP) is `startingBalance + sum(all transactions attributed to this goal)`, mirroring how the daily limit already treats transactions as the source of truth. Percentage rendered as e.g. \"62% to goal\".\n- **Target amount and target date**, always shown as plain text (e.g. \"Target: $2,000 by Dec 1\").\n- **Start date**, always shown as plain text alongside target date when it differs from creation date (see \"Goal creation flow — Start date\" below), e.g. \"Started: Jun 23\" — so a backdated goal's window is visible, not hidden (PROJECT_SPEC trust principle).\n- **\"At current pace\" projection — TWO toggleable views on every active card (JP decision #2, reverses the prior single-definition scoping).**\n\n  A per-card segmented control with two segments, **\"Pace\" (default) / \"Simulation\"**. Per-card, not a global app-wide toggle: each goal already renders as its own independent card (decision #1), so a global toggle would be inconsistent with that pattern — a goal with plenty of spend history and a brand-new goal have different appropriate defaults, and a single global switch would force one choice on both. Toggle state is local UI state (`@State` per card row), not persisted to the model — cheap, no schema change, resets to \"Pace\" each time the screen is opened, which is fine since \"Pace\" is the safe, always-available default.\n\n  **Segment 1 — \"Pace\" (the light, carryForward-sign-based read, unchanged from the original spec):**\n  - Compute the goal's current `dailyLimit` (dailyBase + carryForward, via `DailyLimitCalculator.dailyLimit`) as of today.\n  - If `carryForward \u003e= 0`: \"On pace to meet your goal by \u003ctargetDate\u003e\".\n  - If `carryForward \u003c 0`: \"Behind pace — ~N days behind schedule\", N = `ceil(abs(carryForward) / dailyBase)`.\n  - `dailyBase == 0` guard: show \"Pace unavailable\" instead of dividing by zero.\n  - Reuses existing calculator output as-is — no new simulation loop, cheapest of the two views.\n\n  **Segment 2 — \"Simulation\" (new, heavier: extrapolates actual recent average daily spend forward). Precise definition, grounded only in data `SpendTransaction` actually has (date, amount, kind, savingsGoal):**\n  - **Window**: trailing 14 calendar days before today (today excluded, same convention as `carryForward`'s \"up to but excluding referenceDate's day\"), or fewer if the goal's `effectiveStartDate` is younger than 14 days — window length = `min(14, daysSince(effectiveStartDate, today))`. **Why 14, not 7**: a 7-day window is skewed by which weekday \"today\" happens to be (e.g. a goal checked right after a spendy weekend looks artificially bad); 14 days spans two weekly cycles and smooths that out while staying recent enough to reflect current behavior rather than the goal's entire history. This is a product/UX call, not left to engineer to pick.\n  - **Per-day net**: for each day in the window, `dailyBase - variableSpendThatDay` (variable-kind transactions attributed to this goal only; fixed-kind excluded, same rule `DailyLimitCalculator.carryForward` already applies). A day with zero variable transactions contributes a full `+dailyBase` — consistent with how `carryForward` already treats gap days, not a special case invented for this view.\n  - **avgDailyNet** = sum of per-day nets over the window, divided by the window's day count.\n  - **No-history edge case** (goal created today, or genuinely zero variable spend logged in the window — window day count could be 0 if the goal was created today): do NOT compute an average from zero data points (that would silently imply \"perfectly on pace,\" fabricating a read from an absence of data — the same trust problem the light view was designed to avoid). Instead show: **\"Not enough spending history yet\"** in the Simulation segment, with no numbers. The segmented control itself stays enabled/visible (so the user can see the option exists and check back later) — do not auto-redirect to the Pace segment, which would be a surprising silent switch.\n  - **Output, when computable — both a dollar projection and a date projection (both are directly derivable from avgDailyNet, no need to pick one)**:\n    - `daysRemaining` = whole calendar days from today to `targetDate`.\n    - `projectedSurplusShortfall` = `avgDailyNet * daysRemaining`. Shown as a signed dollar amount: \"Simulation: ~$46 ahead of target by Dec 1\" (avgDailyNet \u003e= 0) or \"~$46 behind target by Dec 1\" (avgDailyNet \u003c 0).\n    - `projectedCompletionDate`: if `avgDailyNet \u003c 0`, `targetDate + ceil(abs(projectedSurplusShortfall) / dailyBase)` days — \"Projected to finish ~5 days late (around Dec 6)\". If `avgDailyNet \u003e= 0`, `targetDate - floor(projectedSurplusShortfall / dailyBase)` days, floored at not going earlier than tomorrow — \"Projected to finish ~3 days early (around Nov 28)\". If `avgDailyNet == 0` exactly, just \"Projected to finish on schedule (Dec 1)\", no date arithmetic needed.\n    - `dailyBase == 0` guard: same as Pace segment, show \"Pace unavailable\" instead of dividing by zero (applies to both segments identically).\n  - This is explicitly a *simulation/extrapolation*, not a guarantee — copy should read \"Simulation: ...\" (the segment label itself signals this is a projection based on recent behavior, distinct from the Pace segment's more conservative on-schedule read). No further hedging copy needed beyond the segment label; over-hedging every projection with disclaimers would clutter a two-view toggle a non-sophisticated user needs to parse quickly.\n\n### Per-goal card, completed-undismissed section\n\nSame as Today's completion banner content: \"Goal target date reached\", target amount, dismiss (X) button. No progress bar/pace projection (neither Pace nor Simulation) — the goal is past its target date, pace is moot.\n\n## Goal creation flow\n\nFields, in a form (reuses StubSheet's sheet-with-NavigationStack shape, replaced with a real form — this bead retires the \"Create a Goal\" StubSheet, wiring TodayView's isShowingCreateGoal and this tab's own create action to the same real sheet):\n- **Target amount** — required, currency input, must be \u003e startingBalance (a target below the starting balance isn't a savings goal; validated inline, not just on submit).\n- **Target date** — required, date picker, must be after today (creation-time \"today\").\n- **Starting balance** — required, currency input, \u003e= 0. Defaults to 0, not blank.\n- **Start date — NOW USER-EDITABLE (JP decision #5, reverses the prior \"fixed to today, non-editable\" scoping).** Date picker, defaults to today, editable to a past date (\"backdating\"):\n  - **Bounds**: must be `\u003c= today` (no future-dating — a future start isn't what \"backdating\" means and adds no value: `targetDate` already must be after both `startDate` and today, so a future `startDate` would just be a slower way to express a normal future-start goal; reject `startDate \u003e today` at validation, inline error \"Start date can't be in the future\"). Lower bound: **no earlier than 90 days before today**, validated inline (\"Start date can't be more than 90 days ago\"). Reasoning: this is a single-goal-cycle personal budgeting tool; JP's stated use case is \"I started two weeks ago\" — 90 days is generous headroom beyond realistic backdating while guarding against a mis-tapped date picker producing a multi-year-old `startDate` that silently wrecks `dailyBase` and the goal's displayed history.\n  - **Mechanical consequence on `dailyBase` — confirmed by reading `DailyLimitCalculator.dailyBase`/`totalDaysFromStart`**: `dailyBase = (targetAmount - startingBalance) / totalDaysFromStart(startDate, targetDate)`. Backdating `startDate` *increases* `totalDaysFromStart` (more whole days between the backdated start and the target), which makes `dailyBase` **smaller** — a softer daily number, not a harsher one. (The original bead's framing guessed the opposite; the actual math favors the user, since spreading the same target amount over more days lowers the daily ask.)\n  - **Carry-forward interaction — this is the part that needed a real decision, not a guess.** `DailyLimitCalculator.carryForward` walks every calendar day from `effectiveStartDate` to today and credits `+dailyBase` to any day with no matching `spendEntries`. `SpendTransaction.savingsGoal` is a to-one relationship set at transaction-entry time — there is no retroactive-attribution UI in this app (no way to pick \"attribute a transaction I logged before this goal existed to this goal\"), so days before the goal's *actual creation* genuinely have zero attributable transactions, not zero real spend. If `effectiveStartDate` were simply the backdated `startDate` (as the calculator's existing signature would do unmodified), every backdated day would be credited a full `+dailyBase` with nothing behind it — e.g. backdating 14 days at a $10/day base hands the user an immediate, fabricated $140 \"banked surplus\" the moment the goal is created. That is a worse trust violation than a harsh number would have been: it's a number shown as real progress that has zero data behind it, directly against PROJECT_SPEC's \"shown, not hidden, to build trust\" principle.\n  - **Decision**: add `createdAt: Date` to `SavingsGoal` (set to `.now` at creation, never user-editable — a genuine data-model addition; see Acceptance Criteria README-trigger note). The plain-value mapping layer (`TodayScreenCalculator`/`GoalsScreenCalculator`) builds `GoalCarryForwardInput.effectiveStartDate` as `lastEditedDate ?? max(startDate, createdAt)`, not `lastEditedDate ?? startDate` alone. `DailyLimitCalculator` itself needs **no code change** — its `effectiveStartDate` parameter already means \"where carry-forward starts accruing\"; this bead just changes what value the mapping layer feeds it when `startDate` is backdated. Net effect: `dailyBase` honestly reflects the longer, softer window the backdated `startDate` implies, while `carryForward` never accrues surplus (or, symmetrically, could never accrue borrowed deficit either) for days that predate the goal's real existence in the app. A backdated goal's very first day therefore behaves like a same-day goal with a gentler `dailyBase` — no artificial windfall, no artificial shortfall.\n  - This is a data-model change (new field) and a calculator-input-mapping change — flagged explicitly because it triggers the README update requirement (STANDARDS §5/§6), and because engineer needs to know `createdAt` is new, not `startDate` repurposed.\n- Submit button disabled until all validations pass; validation errors shown inline under the offending field.\n\nOn submit: create SavingsGoal with `dailyBase` computed via `DailyLimitCalculator.dailyBase(targetAmount:startingBalance:startDate:targetDate:)` (using the possibly-backdated `startDate`), `createdAt = .now`, `lastEditedDate = nil`, `dismissedAt = nil`, save via modelContext, dismiss sheet. Save failure: same rollback + alert pattern as `TodayView.dismiss(_:)` (STANDARDS §3 reuse, don't duplicate).\n\n## Goal edit flow\n\nEditable fields: **targetAmount**, **targetDate**. NOT editable post-creation: **startingBalance**, **startDate**, **createdAt** (changing any of these after the fact has no defined carry-forward semantics — same reasoning as before, now extended to `createdAt` since it's the new carry-forward floor).\n\nEdit is reached via a per-goal edit affordance on the active-goal card (e.g. pencil icon — engineer's call on exact SwiftUI component, resolved below to an explicit button rather than swipe, matching the delete affordance), opens the same form pre-filled, targetAmount/targetDate fields editable, startingBalance/startDate/createdAt fields shown read-only for context but not editable.\n\n**Edit-flow validation (resolved gap from product-lead sanity pass)**: edited targetAmount/targetDate are validated against the same rules as creation — targetAmount must be \u003e startingBalance (unchanged, read-only), targetDate must be after today-at-edit-time and after startDate (unchanged, read-only). Same inline, field-level validation and submit-disabled-until-valid behavior as creation; not a separate rule set.\n\n**Confirmation requirement**: YES, editing targetAmount or targetDate requires an explicit confirmation step before saving, because it resets the carry-forward baseline. Confirmation copy: \"Changing your target will reset today's carry-forward balance. Any amount you're ahead or behind will not carry over. Continue?\"\n\nOn confirmed save: set `lastEditedDate = .now`, recompute `dailyBase` via `DailyLimitCalculator.dailyBase(...)` using the *original* startingBalance/startDate (unchanged) and the *new* targetAmount/targetDate, save. The mapping layer's `effectiveStartDate = lastEditedDate ?? max(startDate, createdAt)` already picks this up correctly post-edit (`lastEditedDate` wins once set) — no further calculator changes needed.\n\n## Goal deletion — IN SCOPE, CONFIRMED (JP decision #3)\n\nReached via a per-goal delete action (explicit icon/menu button, e.g. trash icon — resolved by engineer feasibility pass: swipe-to-delete is ruled out because goal cards render in a `ScrollView { VStack { ForEach } }`, not a `List`, so `.swipeActions` isn't available without a layout restructure, and an explicit button matches the app's existing destructive-action convention, i.e. the \"X\" dismiss button on completed goals).\n\n- **Requires confirmation** (destructive, irreversible): \"Delete this goal? Its N attributed transactions will no longer count toward any daily limit, but will not be deleted.\" (N computed from goal.transactions.count, omit the transaction clause if N == 0).\n- **Attributed SpendTransactions are NOT deleted** — the model's relationship is `@Relationship(deleteRule: .nullify, inverse: \\SpendTransaction.savingsGoal)`, so deleting a SavingsGoal already nullifies `SpendTransaction.savingsGoal` at the SwiftData layer with no code change needed; transactions become orphaned (savingsGoal == nil), same status TodayScreenCalculator already treats as \"always counts toward spentToday regardless of attributedGoals.\" This is existing, tested behavior — deletion doesn't require new calculator logic, only the UI action + modelContext.delete(goal) + save (with the same rollback-on-failure pattern as dismiss/create).\n\n## Technical approach (product-lead notes for engineer)\n\n- **Reuse, don't duplicate**: `DailyLimitCalculator.dailyBase`, `.dailyLimit`, `.carryForward` already provide everything the Pace segment needs. The Simulation segment's avgDailyNet/projection math is new pure logic (see calculator-placement decision below).\n- **`createdAt` addition — RESOLVED by engineer feasibility pass**: new `SavingsGoal.createdAt: Date`, non-optional, default `.now`, set once at init, never mutated. Requires a real **SchemaV3** with a `.lightweight` `MigrationStage` (same pattern as the existing V1→V2 migration in `Reservoir/Models/Migrations/`) — a non-optional stored property added to an existing `@Model` can't be silently absorbed by the current schema iteration. Giving it a default (`= .now`) in the model declaration lets lightweight/inferred migration handle it without a custom stage. **Flagged consequence, acceptable given no released users**: any goal that exists before this migration lands gets `createdAt` backfilled to the migration run's timestamp, which — under `effectiveStartDate = max(startDate, createdAt)` — retroactively zeroes that goal's carry-forward history the moment the app updates. Since this is pre-release/personal use with no existing installed base to protect, this is a one-time, acceptable side effect, not a blocker; noting it explicitly rather than leaving it implicit.\n- **Mapping layer**: `effectiveStartDate = lastEditedDate ?? max(startDate, createdAt)` replaces the prior `lastEditedDate ?? startDate` wherever `GoalCarryForwardInput` is constructed from a `SavingsGoal` (currently in `TodayScreenCalculator`, and wherever this bead's Goals-screen calculator builds the same input). **This changes already-shipped, already-tested logic** (PR #3, reservoir-adq.2) — existing `TodayScreenCalculatorTests` covering the old `lastEditedDate ?? startDate` formula must be re-verified/updated against the new formula as part of this bead, not just supplemented with new backdating-specific tests.\n- **Calculator placement — RESOLVED by engineer feasibility pass**: new **`GoalsScreenCalculator`**, not an extension of `TodayScreenCalculator`. The Simulation segment's avgDailyNet/projection math is substantial new pure logic with no natural home in a file already scoped to \"Today screen: lifecycle + summary.\" Goal lifecycle primitives (`activeGoals`, `completedUndismissedGoals`, `isGoalMet`, the `GoalCarryForwardInput` mapping) stay in `TodayScreenCalculator` as the single source of truth; `GoalsScreenCalculator` calls into those rather than duplicating them — two SwiftData-aware mapping layers under the same STANDARDS §4 exception, each scoped to the screen that owns its unique math.\n- **Dismiss/create/edit/delete save pattern**: extract `TodayView.dismiss(_:)`'s mutate/try-save/rollback/alert shape into something shared rather than four independent copies (STANDARDS §3).\n- **StubSheet retirement**: removes `CreateGoalStubSheet` usage from both `TodayView` and the new Goals tab; `StubSheet` itself stays for Add Transaction/Settings stubs.\n- **`dailyBase == 0` guard — clarified by product-lead sanity pass**: given creation validation requires `targetAmount \u003e startingBalance` and `targetDate \u003e startDate` (both strict), this guard is not reachable through the validated UI flow — it is a defensive guard inherited from `DailyLimitCalculator`'s existing signature, exercised via direct unit test on the calculator/mapping layer, not a state a tester should expect to reproduce by driving the UI. Do not weaken creation/edit validation trying to make this guard UI-reachable.\n\n## Product decisions log (all five prior open questions, now resolved)\n\n1. **Multi-goal display**: one card per goal — confirmed.\n2. **\"At current pace\" definition**: BOTH the light carryForward-sign read and a heavier 14-day average-spend simulation, toggleable per-card via a segmented control (\"Pace\" / \"Simulation\", default \"Pace\") — see full spec above.\n3. **Goal deletion**: in scope — confirmed.\n4. **Dismissed-goal history**: goals vanish entirely on dismiss, no history view — confirmed.\n5. **Start date editability**: now user-editable at creation (backdating), bounded to `\u003c= today` and `\u003e= today - 90 days`, with a new `SavingsGoal.createdAt` field added specifically so carry-forward accrual floors at the goal's real creation date rather than the user-chosen backdated `startDate` — see full mechanical spec above.\n\n## Engineering questions resolved (2026-07-08 sanity pass, product-lead + engineer)\n\nThe three items previously flagged \"engineer's call, not deciding unilaterally\" are now resolved and folded into the sections above:\n- `createdAt` migration: SchemaV3 + lightweight MigrationStage, default `.now`; pre-existing goals backfill to migration timestamp (flagged, acceptable pre-release).\n- Calculator placement: new `GoalsScreenCalculator` for goal-specific math (currentBalance, avgDailyNet/simulation); lifecycle primitives stay in `TodayScreenCalculator`, called into rather than duplicated.\n- Delete/edit affordance: explicit icon buttons (not swipe) for both — goal cards aren't in a `List`, so `.swipeActions` isn't available without a layout change; matches the app's existing explicit-button convention (Today's \"X\" dismiss).\n\nAlso fixed from product-lead's sanity pass: edit-flow validation was previously unspecified (now explicit — same rules as creation); `dailyBase == 0` guard clarified as defensive/unreachable via validated UI, not a state to hunt for in manual testing; existing `TodayScreenCalculatorTests` must be updated (not just supplemented) for the `effectiveStartDate` formula change, since it touches already-shipped logic from PR #3.\n\n**Separately flagged, not part of this bead's scope**: reservoir-adq.7 (Settings screen)'s own description still lists \"goal management (create/edit SavingsGoal...)\" as its scope, which is now fully redundant with this bead's full CRUD. adq.7 already depends on adq.5, suggesting Settings should link into these flows rather than reimplement them, but adq.7's description wasn't updated to say so — flagged via a comment on adq.7, not resolved here.\n","acceptance_criteria":"- [ ] Goals tab lists active goals (sorted by targetDate ascending) and completed-undismissed goals, reusing TodayScreenCalculator.activeGoals/completedUndismissedGoals — no second lifecycle model invented for this screen. Dismissed goals never appear anywhere in this bead (no history view).\n- [ ] Zero-goals empty state matches Today's NoActiveGoalPrompt pattern (same copy/spirit), reached from both tabs.\n- [ ] Active-goal card is one-card-per-goal (no consolidated multi-goal view) and shows: progress bar (clamped 0-1) + percentage text, target amount, target date, start date, and a per-card segmented control (\"Pace\" / \"Simulation\", default \"Pace\") switching between the two pace-projection reads.\n- [ ] \"Pace\" segment: carryForward-sign-based on-pace/behind-pace text exactly as specified (on pace by \u003ctargetDate\u003e if carryForward \u003e= 0; \"~N days behind schedule\" if \u003c 0, N = ceil(abs(carryForward)/dailyBase)).\n- [ ] \"Simulation\" segment: computes avgDailyNet over a trailing-14-day (or shorter, if goal younger than 14 days) window of variable-kind spend attributed to the goal; outputs both a projected surplus/shortfall dollar amount at targetDate and a projected completion date, per the exact formulas in the description. Zero-data window (goal created today / no spend logged) shows \"Not enough spending history yet\" instead of fabricating a zero-average read — control stays visible/enabled, no auto-redirect to Pace segment.\n- [ ] dailyBase == 0 edge case (same-day start/target) shows \"Pace unavailable\" in both Pace and Simulation segments instead of dividing by zero or crashing. Unit tests cover this for both segments.\n- [ ] Unit tests for Simulation segment cover: window truncation for goals younger than 14 days, the zero-history edge case, avgDailyNet \u003e= 0 (ahead/early) and \u003c 0 (behind/late) branches, and the dollar/date projection formulas.\n- [ ] Completed-undismissed goal card matches Today's completion-banner content/copy and dismiss (X) action; no progress bar or pace projection (neither segment) shown. Dismiss reuses the same modelContext.save() + rollback-on-failure + error alert pattern as TodayView.dismiss(_:), not a duplicated implementation (STANDARDS §3).\n- [ ] Goal creation form: targetAmount (required, \u003e startingBalance), targetDate (required, after today), startingBalance (required, \u003e= 0, defaults to 0), startDate (required, defaults to today, editable/backdatable, must be \u003c= today and \u003e= today-90 days — both bounds validated inline with the exact error copy in the description). Submit disabled until valid.\n- [ ] Goal creation replaces the \"Create a Goal\" StubSheet: both TodayView's empty-state \"Create a goal\" button and the Goals tab's own create entry point open the same real sheet (STANDARDS §3, no copy-paste).\n- [ ] SavingsGoal gains a new `createdAt: Date` field (non-optional, default `.now`), landed via a new SchemaV3 + lightweight MigrationStage (matching the existing V1→V2 migration pattern), never user-editable and never shown as editable in the edit form. This is a data-model change — README updated per STANDARDS §5/§6 to document the new field and why it exists (carry-forward accrual floor, decoupled from the user-facing, backdatable `startDate`), including a note that pre-existing goals get `createdAt` backfilled to the migration timestamp (acceptable, no released users to protect).\n- [ ] Existing `TodayScreenCalculatorTests` covering the pre-backdating `effectiveStartDate = lastEditedDate ?? startDate` formula are reviewed and updated (not just supplemented) to reflect the new `lastEditedDate ?? max(startDate, createdAt)` formula, confirming no regression to already-shipped Today-screen carry-forward behavior.\n- [ ] On create: dailyBase computed via DailyLimitCalculator.dailyBase(...) using the (possibly backdated) startDate; createdAt = .now; lastEditedDate = nil; dismissedAt = nil.\n- [ ] The SwiftData -\u003e GoalCarryForwardInput mapping (TodayScreenCalculator and/or GoalsScreenCalculator) computes effectiveStartDate as `lastEditedDate ?? max(startDate, createdAt)`, not `lastEditedDate ?? startDate`. Unit/integration test confirms: a goal backdated e.g. 14 days with zero attributed transactions shows carryForward == 0 on creation day (no fabricated windfall), and dailyBase reflects the full backdated-to-target day count (smaller/softer than an equivalent same-day-start goal with the same target/amount).\n- [ ] Goal edit form: targetAmount/targetDate editable; startingBalance/startDate/createdAt shown read-only, not editable. Reached via a per-goal edit action (explicit button, e.g. pencil icon — not swipe, per resolved SwiftUI-affordance question).\n- [ ] Edited targetAmount/targetDate are validated against the same rules as creation (targetAmount \u003e startingBalance; targetDate \u003e today-at-edit-time and \u003e startDate), inline field-level errors, submit disabled until valid — not left unvalidated.\n- [ ] Editing targetAmount or targetDate requires an explicit confirmation dialog before saving, with copy that plainly states carry-forward will reset (exact requirement, not left as an implementation detail).\n- [ ] On confirmed edit save: lastEditedDate = .now; dailyBase recomputed via DailyLimitCalculator.dailyBase(...) using unchanged startingBalance/startDate and new targetAmount/targetDate; effectiveStartDate = lastEditedDate ?? max(startDate, createdAt) mapping picks this up (lastEditedDate wins) with no DailyLimitCalculator changes needed.\n- [ ] Goal deletion: per-goal delete action (explicit icon/menu button, not swipe — goal cards render in a ScrollView/VStack/ForEach, not a List, so .swipeActions isn't available without a layout change) with a confirmation dialog stating attributed transaction count and that transactions are preserved (orphaned), not deleted. Deletion calls modelContext.delete(goal) + save with the same rollback-on-failure/error-alert pattern as create/dismiss/edit.\n- [ ] Deleting a goal orphans its SpendTransactions (savingsGoal == nil) via the existing SchemaV1/V2 .nullify delete rule; a unit or integration test confirms transactions survive goal deletion and still count in TodayScreenCalculator.spentToday as orphaned transactions.\n- [ ] Unit tests (business-logic target, 80% coverage per STANDARDS §5) cover: progress percentage math (including \u003e100% and negative cases), Pace segment on-pace/behind-pace branching and N-days-behind formula, Simulation segment per above, dailyBase==0 guard (both segments), createdAt-floored effectiveStartDate mapping (including the backdated-with-no-transactions case), creation validation (all four fields including startDate's two bounds), edit recompute of dailyBase/lastEditedDate.\n- [ ] XCUITest coverage (STANDARDS §5) for: goal list rendering (active + completed-undismissed sections), create-goal happy path including backdating startDate, create-goal validation error paths (including startDate out-of-bounds), Pace/Simulation segmented-control toggle on a goal card, edit-goal confirmation-and-save path, delete-goal confirmation-and-save path.\n- [ ] README updated (STANDARDS §5/§6) — this bead adds a real Goals screen, goal CRUD, a new SavingsGoal.createdAt model field, and backdatable startDate — all features/architecture/data-model changes that trigger the README update requirement.\n- [ ] Commit scope naming: `goal-progress` (matches STANDARDS §2's example scope) for calculator/projection work; `goals` or similarly-named scope for the screen/CRUD UI work — engineer's call on the exact split, but consistently scoped so commits map to this bead.","notes":"Updated 2026-07-07: folded in all five JP decisions from the 2026-07-08 comment. Two required real spec work: (2) dual pace views — Pace (unchanged carryForward-sign read) + new Simulation segment (14-day trailing avg-spend extrapolation, per-card segmented control); (5) backdating startDate — bounded [today-90, today], new SavingsGoal.createdAt field added so carry-forward accrual floors at real creation date, not the backdated startDate (prevents fabricated carry-forward windfall for pre-creation days). Bead left open/unclaimed.","status":"closed","priority":2,"issue_type":"feature","assignee":"johnpease","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:54Z","created_by":"johnpease","updated_at":"2026-07-11T05:31:55Z","closed_at":"2026-07-11T05:31:55Z","close_reason":"PR #5 merged to develop 2026-07-11 (https://github.com/JohnPease/reservoir/pull/5). Goals screen fully implemented: progress/pace/simulation projections, full goal CRUD, SchemaV3 migration for createdAt, banked-surplus currentBalance model (reservoir-1et). Twice code-reviewed, all correctness findings and cleanup items resolved, 139/139 tests passing.","dependencies":[{"issue_id":"reservoir-adq.5","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.5","depends_on_id":"reservoir-adq.2","type":"blocks","created_at":"2026-07-07T00:10:54Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":3}
{"id":"reservoir-adq.3","title":"Manual transaction entry + Transactions list","description":"Transactions tab: list of imported/manual transactions with fixed/variable tagging, manual entry flow, and MerchantRule management. Replaces the Today screen's `AddTransactionStubSheet` stub and the `RootTabView` \"Transactions\" placeholder `Text`. Depends only on data models (reservoir-adq.1, done); does not require the calculator.\n\n## Scope note: retroactive re-tagging now lives in this story (2026-07-07 decision)\n\nJP decided MerchantRule create/edit **does** retroactively re-tag existing matching transactions (see comment log). That means the core merchant-match engine is needed immediately by this story's own MerchantRule CRUD UI, not just by adq.4's import-time flow. Scope split:\n\n- **This story (adq.3) owns**: the merchant-name-match function (`[MerchantRule] -\u003e String -\u003e TransactionType?`) as a pure, testable `Services/` type, AND both of its current call sites: (1) the manual-entry form's auto-suggest, (2) the retroactive retag pass triggered synchronously on MerchantRule create/edit.\n- **adq.4 owns**: applying that same function at *import time* only (when Plaid delivers new transactions, future bead reservoir-adq.6+). adq.4 does not rebuild the matching engine — it depends on this story's `Services/` type and calls it. See adq.4's description for its narrowed scope.\n\nThis is option (a) from the reconciliation: pull the matching engine into adq.3 since MerchantRule CRUD needs it now, leave adq.4 as a thin import-time consumer of the same function. Rationale: building the match function twice (or building it here and having adq.4 duplicate it) would violate STANDARDS §3 (no copy-paste) and risk the two call sites drifting into different matching semantics (e.g. one going case-insensitive-exact, the other creeping toward substring match) — one shared implementation forecloses that.\n\n**Known open issue, deliberately deferred (not dropped):** the manual-entry form's auto-suggest and the retroactive-retag-on-rule-save pass both call the same matching function, but *how* they're wired (do they literally call one shared function today, or do they happen to implement the same logic twice for now with a TODO to unify) is JP's call to make later, not resolved in this story. Engineer should implement one shared pure function and have both call sites use it — that satisfies both the immediate need and leaves nothing to unify later. Flagging only because JP explicitly said not to silently drop the question.\n\n## Data model (already in place — SchemaV2, no changes needed for this story)\n\n- `SpendTransaction`: `amount`, `date`, `merchantName`, `type` (variable/fixed), `entryMethod` (manual/imported), `plaidTransactionID` (nil for manual), `isManualOverride` (Bool — true once a user hand-sets/changes `type`, so future MerchantRule re-application doesn't clobber it), `savingsGoal` (optional `SavingsGoal?` relationship — nil is a valid \"unattributed\" state), `createdAt`.\n- `MerchantRule`: `merchantName` (exact, case-insensitive match), `type` (variable/fixed). **No goal-attribution field, and none is being added.** (2026-07-12 resolution: JP and orchestrator settled the former \"open question 3\" — no `defaultGoal` field, no SchemaV3, goal attribution stays manual/per-transaction for MVP. Rationale: attribution only matters once 2+ SavingsGoals are concurrently active with independent carry-forwards, and PROJECT_SPEC.md marks multi-goal support Phase 2, not MVP. See NOTES for full reasoning. This is settled, not open — do not resurface during implementation.)\n- `SavingsGoal`: unchanged, used only for the goal-attribution picker.\n\n## UX / requirements\n\n### Manual transaction entry (add + edit, same form)\nFields:\n- **Amount** — required, numeric keypad, must be \u003e 0 (existing validation rule, spec §\"Empty states and validation\").\n- **Date** — defaults to today; editable via date picker. Bound: **must be today or in the past**, not future. Reasoning: a transaction represents money already spent; a future-dated entry would silently inflate \"spent today\"/carry-forward math on a day that hasn't happened yet, corrupting the core mechanic's trust (PROJECT_SPEC \"Core mechanic\" — this is the same trust concern that made dedup a Phase-1, not deferred, concern). No lower bound — backdating a cash purchase from days ago is a legitimate fallback use case per the spec's \"needed as fallback even with Plaid.\"\n- **Merchant / description** — required free-text field, non-empty after trimming whitespace.\n- **Type (variable/fixed)** — segmented control, defaults to `variable`. If the entered merchant name exact-matches an existing `MerchantRule` (case-insensitive), auto-select that rule's `type` when the merchant field loses focus/on save, but let the user override it in the same form. If the user changes the type away from what the rule would set, mark `isManualOverride = true` on save (this is what protects the entry from later auto-retag passes). If no rule matches, default `variable` and `isManualOverride = false`. This auto-suggest uses the same merchant-match function as the retroactive retag pass below (see scope note above).\n- **Goal attribution** — picker of active `SavingsGoal`s (see `TodayScreenCalculator.activeGoals`). Behavior: if exactly one active goal exists, pre-select it (common case, minimize taps for the target user). If zero or more than one active goal exists, default to **unattributed** and require the user to either pick a goal or explicitly confirm \"unattributed\" — do not silently guess among multiple goals, since misattribution feeds directly into the wrong goal's carry-forward. Unattributed is always a legal end state (e.g. no goal exists yet, or the spend genuinely isn't tied to any goal) — the transaction still saves and still counts toward the day's total spend.\n  - **Zero-active-goals case, clarified 2026-07-12** (flagged by product review pass): the Today screen's \"Add transaction\" entry is unreachable with zero goals (Today shows a goal-creation prompt instead), but the Transactions tab's own independent \"+\" entry point *is* reachable with zero goals. In that state the goal picker has no real choices — it should render as \"Unattributed\" pre-selected and effectively a no-op confirm (no goal list to pick from, just an acknowledged unattributed state), not an empty/broken-looking picker. This is a UI-state clarification, not a scope change.\n- **Category** — **not a field.** Not in the data model (PROJECT_SPEC's `Transaction` table has no category column) and not asked for anywhere in the spec's UX section. Adding one here would be scope creep beyond what the spec locked in.\n\nEntry point: the existing \"Add transaction\" button on the Today screen (`TodayView.isShowingAddTransaction`, currently `StubSheet`) now presents this real form instead of the stub. The Transactions tab also gets its own \"+\" entry point (toolbar button) for adding a transaction without going through Today — both present the same `TransactionEntryView` sheet.\n\nEditing: tapping a row in the Transactions list opens the same form pre-filled, including reassigning `savingsGoal` after the fact (e.g. correcting a misattributed spend) — low-stakes, no reason to lock it down once creation already allows free-form attribution. Deleting: swipe-to-delete on the list row, with a confirmation alert (irreversible; SwiftData hard-deletes) — no soft-delete/undo in MVP, matches the app's overall lack of an undo system elsewhere.\n\n### Transactions list\n- Sorted **date descending** (newest first, matching `TodayView`'s existing convention: `SortDescriptor(\\.date, order: .reverse)` then `\\.createdAt, order: .reverse)` as the same-day tiebreak).\n- **Grouped by day** (section headers, e.g. \"Today,\" \"Yesterday,\" then full dates) — a flat list of amounts with no day boundary is hard to scan for a user who isn't financially sophisticated; day grouping is a low-cost SwiftUI `List`/`Section` pattern that directly supports \"how much did I spend on X day.\"\n- Each row shows: type icon (reuse `TodayView.TransactionRow`'s `lock.fill`/`cart.fill` convention), merchant name, time, amount, and a muted \"Excluded from limit\" caption + reduced opacity for `fixed` transactions (same visual language as the Today screen's recent-transactions section — consistency matters more than novelty here). Also show a small goal-attribution indicator (goal name or \"Unattributed\") since this view, unlike Today's abbreviated recent list, is where the user audits attribution.\n- **Filter**: a segmented control (All / Variable / Fixed) at the top of the list. Recommended and in-scope: cheap to build (a `Picker` + local filter, no new query), and directly useful for \"how much of my spending this month was fixed vs. variable\" — a question this exact user persona (budget-conscious, tracking a rolling limit) will actually ask.\n- **Search**: **out of scope for this story.** A single-user personal ledger with modest transaction volume doesn't yet justify a search bar; the day-grouped list is enough to scan. Revisit if transaction volume after Plaid import (adq.6) makes scanning impractical.\n\n### MerchantRule management (lives in this tab, per spec IA)\n- A \"Merchant rules\" list, reachable via a toolbar/nav-link from the Transactions tab (not its own top-level tab — the spec's 4-tab IA is locked).\n- List shows each rule: merchant name + type tag (variable/fixed).\n- **Create**: \"+\" button opens a form with merchant name (required, non-empty, trimmed) and type (variable/fixed, required, no default — forces an explicit choice since this is a rule that will silently tag transactions, both new and retroactively).\n- **Edit**: tap a rule to change its merchant name or type.\n- **Delete**: swipe-to-delete, with confirmation. Deleting a rule does **not** retroactively untag/revert previously-retagged transactions — those keep whatever `type` they were last set to. Only create/edit trigger a forward retag pass; delete is inert with respect to existing transaction data. (Asymmetric on purpose: reverting a retag would require tracking \"what would this transaction's type have been without the rule,\" which isn't in the data model and isn't worth adding for MVP.)\n- Duplicate merchant names: reject on save (case-insensitive) with an inline error — two rules for the same merchant string is an unresolvable conflict, not a case to silently allow last-write-wins.\n- **Retroactive retag on create/edit (new in this story per JP's 2026-07-07 decision)**: when a MerchantRule is created, or edited such that its `merchantName` or `type` changes, immediately walk all existing `SpendTransaction`s where `merchantName` case-insensitively matches the rule's `merchantName` AND `isManualOverride == false`, and set their `type` to the rule's `type`. Transactions with `isManualOverride == true` are never touched by this pass — that flag exists specifically to protect a user's explicit choice from being silently overwritten by a later rule change. This pass runs synchronously as part of the rule save (small personal dataset, no background job needed). **What's still adq.4's job**: applying this same match function when Plaid imports new transactions (a different trigger — import, not rule CRUD — and a different bead since it depends on the not-yet-built Plaid integration, adq.6).\n\n## Technical approach\n\n- New feature folder `Reservoir/Features/Transactions/` (STANDARDS §4, organize by feature not layer):\n  - `TransactionsView.swift` — the tab root: filter control, day-grouped `@Query`-backed list, toolbar \"+\" for add, nav link to merchant rules.\n  - `TransactionEntryView.swift` — add/edit form, used both from `TransactionsView`'s \"+\" and from `TodayView`'s \"Add transaction\" sheet. Takes an optional existing `SpendTransaction` (nil = create, non-nil = edit) so one view serves both, per STANDARDS §3 no-copy-paste.\n  - `MerchantRulesView.swift` — rule list.\n  - `MerchantRuleEntryView.swift` — add/edit rule form; on save, invokes the retag pass described above.\n- Wire into `Reservoir/App/RootTabView.swift`: replace the placeholder `Text(\"Transactions\")` with `TransactionsView()`.\n- Wire into `Reservoir/Features/Today/TodayView.swift`: replace the `StubSheet` in `.sheet(isPresented: $isShowingAddTransaction)` with `TransactionEntryView()` (create mode, no goal preselected beyond the same active-goal default logic described above — reuse, don't reimplement).\n- Any non-trivial logic here (rule matching against merchant name, retroactive retag pass, \"is this the sole active goal\" default, date-bound validation) should be pure functions in `Services/`, not inlined in the views, per STANDARDS §3/§4 — likely a small `TransactionEntryValidator` or similar (naming per engineer's judgment, following the `*Calculator`/`*Validator` suffix convention in STANDARDS §4). Flag to engineer: given the goal-default-picking logic already exists as `TodayScreenCalculator.activeGoals`, reuse that rather than re-deriving \"active goal\" logic in a second place.\n- The merchant-name-match function (`[MerchantRule] -\u003e String -\u003e TransactionType?`) and the retag-application function (takes matched rule + existing transactions, returns which to update) should both be pure, testable `Services/` types — `MerchantRule` CRUD (create/edit/delete themselves) is otherwise straightforward `@Model` mutation via `modelContext`, no new pure-logic type needed for the CRUD mechanics alone.\n- Flag to engineer: adq.4 depends on the matching function built here. Name/expose it so it's reusable from a different feature folder (e.g. don't bury it as a private type inside `TransactionEntryView.swift`).\n- **Kickoff checks added 2026-07-12 (engineering review pass), confirm or flag back before/while implementing — none block starting, but resolve rather than discover mid-build:**\n  1. Verify `TodayScreenCalculator.activeGoals` is actually a standalone, non-private `[SavingsGoal] -\u003e [SavingsGoal]`-shaped pure function (or trivially extractable to one) before assuming it's reusable as-is from the new `Transactions` feature folder. If it's coupled to `TodayScreenCalculator`'s instance state/today's-date context rather than a plain filter, do a small extraction refactor rather than re-deriving \"active goal\" logic in a second place.\n  2. The retag pass (rule save + batch transaction updates) should be a single atomic `modelContext.save()` after all mutations (rule + retagged transactions), not two sequential saves — a partial failure (rule saved, retag not applied) would leave the \"immediately retags\" contract broken.\n  3. The retag pass must not re-fire on a MerchantRule edit that changes neither `merchantName` nor `type` (a no-op save). Diff old vs. new rule state inside the same pure retag-application function (so it's covered by the same unit tests) rather than leaving \"did anything change\" logic to the view layer.\n  4. No background task/async plumbing needed for the retag pass — single-user, synchronous, main-actor SwiftData save is sufficient; don't over-engineer concurrency handling here.\n\n## Acceptance criteria\n\n- [ ] Transactions tab replaces the placeholder `Text(\"Transactions\")` in `RootTabView` and shows a day-grouped, date-descending list of all `SpendTransaction`s.\n- [ ] Each row shows type icon, merchant, time, amount, goal attribution (or \"Unattributed\"), and fixed transactions render muted with \"Excluded from limit,\" matching `TodayView`'s existing visual convention.\n- [ ] Empty state (no transactions) shows a message, not a blank list.\n- [ ] All/Variable/Fixed filter control works and is covered by an XCUITest.\n- [ ] \"+\" on the Transactions tab and \"Add transaction\" on the Today screen both open the same real `TransactionEntryView` (Today's stub sheet removed).\n- [ ] Entry form validates: amount \u003e 0; date \u003c= today (device local, `Calendar.current`); merchant name non-empty after trim. Invalid input blocks save and shows an inline, field-level error (not a generic alert) — covered by unit tests for the validation logic and at least one XCUITest exercising a validation failure end to end.\n- [ ] Goal attribution picker: auto-selects the sole active goal when exactly one exists; otherwise requires an explicit choice (including \"Unattributed\") before save is enabled. Covered by a unit test on the selection-default logic.\n- [ ] Merchant-name match against existing `MerchantRule`s auto-fills `type` on the entry form (case-insensitive exact match), user can still override, and overriding sets `isManualOverride = true`. Covered by a unit test on the match/override logic.\n- [ ] **(Added 2026-07-12, product review pass)** When no `MerchantRule` matches the entered merchant name, the entry form defaults to `type = variable` and `isManualOverride = false` — covered by a unit test asserting this no-match default explicitly (previously only the match/override path had a stated test).\n- [ ] **(Added 2026-07-12, product review pass)** With zero active `SavingsGoal`s, the goal-attribution picker on the Transactions tab's \"+\" entry (reachable even with no goals, unlike Today's) renders \"Unattributed\" pre-selected with no goal list to choose from, and save proceeds without requiring further input.\n- [ ] **Retroactive retag**: creating or editing a MerchantRule (name or type change) immediately retags all existing `SpendTransaction`s with a case-insensitive matching `merchantName` and `isManualOverride == false` to the rule's `type`; transactions with `isManualOverride == true` are left untouched. Covered by a unit test with a mixed fixture (some matching transactions manually overridden, some not) asserting only the non-overridden ones change. Deleting a rule does not retroactively change any transaction.\n- [ ] The merchant-match function and the retag-application function are pure `Services/` functions/types (not inlined in views), unit tested, and structured so reservoir-adq.4 can call the match function directly for its import-time use case without reimplementing it.\n- [ ] Editing an existing transaction (including reassigning its goal) persists correctly; deleting requires confirmation and removes the record.\n- [ ] Merchant rules list (create/edit/delete) is reachable from the Transactions tab, rejects duplicate merchant names (case-insensitive) with an inline error, and requires an explicit type selection on create (no silent default).\n- [ ] Business logic (validation, goal-default selection, merchant-match lookup, retag pass) lives in `Services/` as SwiftUI-free pure functions/types, unit tested to STANDARDS §5's 80% bar.\n- [ ] XCUITest coverage for: add a transaction end to end, edit a transaction, delete a transaction, create/edit/delete a merchant rule, and creating a rule that retroactively retags at least one existing transaction visible in the list.\n- [ ] README updated (STANDARDS §5/§6) — this story adds a real feature (Transactions tab, MerchantRule UI, retroactive retag behavior) replacing a stub, which is exactly the \"features changed\" trigger for a README update.\n- [ ] Commits scoped `feat(transactions)` / `feat(merchant-rules)` per STANDARDS §2 (matches the feature areas this story defines — use `merchant-rules` as its own scope since it's a distinct enough surface, engineer's call if a single `transactions` scope covers both better in practice).\n\n","notes":"Open product questions flagged for JP (not decided unilaterally — everything else in this story's description reflects a call I made and justified inline):\n\n1. **RESOLVED 2026-07-07** — Does creating/editing a MerchantRule retroactively re-tag existing transactions? JP decided: yes. Retroactive re-tagging of existing non-manually-overridden transactions is now built into this story (adq.3), triggered synchronously on MerchantRule create/edit. adq.4 is narrowed to import-time application of the same matching function only. See this story's \"Scope note\" section and acceptance criteria for the full split.\n\n2. **Deliberately deferred, not dropped** — should the manual-entry form's auto-suggest and the retroactive-retag-on-rule-save pass share one literal matching implementation, or is it acceptable if they happen to duplicate the same logic for now? JP said to ignore this for now. Documented as a known open issue in this story's description (\"Known open issue, deliberately deferred\") — engineer should default to one shared pure function for both call sites since that satisfies the requirement either way, but the explicit product decision on this is still outstanding. STILL OPEN as of 2026-07-12 refinement pass — not resolved in this pass either, per explicit instruction not to touch it.\n\n3. **RESOLVED 2026-07-12** — Should `MerchantRule` carry a default `SavingsGoal` for auto-attribution (requiring a SchemaV3 migration)? JP decided: no. Do not add a `defaultGoal` field; proceed with adq.3 as scoped (goal attribution stays manual, per-transaction). Reasoning: goal attribution only matters once 2+ SavingsGoals are simultaneously active (each has independent, non-pooled carry-forward); PROJECT_SPEC.md marks multi-goal support as Phase 2, not MVP, so the premise this question depends on isn't guaranteed during MVP usage. A per-merchant default goal is also a fairly specific usage pattern that may not match how JP actually uses multiple goals if/when he has them. Revisit only if JP runs 2+ goals concurrently and finds manual attribution genuinely annoying in practice. See description's \"Data model\" section for the pointer, and the 2026-07-12 comment for full reasoning.\n\n## 2026-07-12 refinement pass (product-lead + engineer review, orchestrator-run)\n\nStory reviewed for completeness/consistency against docs/PROJECT_SPEC.md and STANDARDS.md before engineering starts. Verdict from both reviewers: ready for engineering, with the following small additions folded into the description/acceptance criteria (not re-specs, just closing gaps):\n\n- Added AC: no-match default (`variable`, `isManualOverride = false`) needs its own explicit unit test, not just the match/override path.\n- Added UX clarification + AC: zero-active-goals state for the Transactions tab's independent \"+\" entry (reachable with zero goals, unlike Today's) — picker renders \"Unattributed\" pre-selected, no goal list, no blocking confirm needed.\n- Added technical clarifications to \"Technical approach\": verify `TodayScreenCalculator.activeGoals` is actually a reusable standalone pure function before assuming import (small extraction refactor may be needed if it's coupled to the view); retag pass should be one atomic `modelContext.save()` covering rule + retagged transactions, not two sequential saves; retag pass must not fire on a no-op MerchantRule edit (diff old vs. new state inside the pure retag-application function); no background task/async needed for the retag pass (single-user, synchronous, main-actor is sufficient).\n- No new open product questions surfaced by this pass. Question 2 remains open exactly as before (untouched, per instruction). Question 3 is now resolved (see above).\n- Nothing flagged as scope creep or under-scoped beyond the two AC additions above; category-field and search exclusions were re-confirmed as correct calls.","status":"in_progress","priority":2,"issue_type":"feature","assignee":"johnpease","owner":"johnjpease@gmail.com","created_at":"2026-07-07T05:10:38Z","created_by":"johnpease","updated_at":"2026-07-12T14:09:14Z","dependencies":[{"issue_id":"reservoir-adq.3","depends_on_id":"reservoir-adq","type":"parent-child","created_at":"2026-07-07T00:10:38Z","created_by":"johnpease","metadata":"{}"},{"issue_id":"reservoir-adq.3","depends_on_id":"reservoir-adq.1","type":"blocks","created_at":"2026-07-07T00:10:38Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":5}
{"id":"reservoir-4w8","title":"GoalsScreenUITests.testCreateGoalStartDateInFutureShowsError hardcodes wall-clock 'today', flakes as date advances","description":"ReservoirUITests/GoalsScreenUITests.swift:142 hardcodes a specific day-of-month intended to mean 'tomorrow' relative to when the test was written, assuming a fixed wall-clock 'today'. It has now flaked twice for this exact reason: once when today advanced from July 8 to July 9 (noted during the PR #5 first fix-commit pass), and again when today advanced to July 10 (noted during the reservoir-1et banked-surplus fix verification). The test doesn't mock/inject 'today' the way GoalsScreenCalculatorTests and TodayScreenCalculatorTests do (both use an injected reference date) — it relies on the simulator's live clock. Fix: compute the target date relative to Date() at test-run time (e.g. calendar.date(byAdding: .day, value: 1, to: .now)) instead of a hardcoded day-of-month, so the test doesn't depend on which day it happens to run.","status":"closed","priority":3,"issue_type":"bug","owner":"johnjpease@gmail.com","created_at":"2026-07-10T12:39:51Z","created_by":"johnpease","updated_at":"2026-07-11T04:01:10Z","closed_at":"2026-07-11T04:01:10Z","close_reason":"Fixed in commit 2c0349e (feature/goals-screen, PR #5): testCreateGoalStartDateInFutureShowsError now computes its target date via calendarOffset(forDaysFromToday:) relative to Date() at test-run time instead of a hardcoded day-of-month. Verified: full suite 139/139 pass.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"reservoir-3fc","title":"Add test coverage for Simulation completion outcome when goal is behind pace and due today","description":"Follow-up from the Simulation completion-date fix (commit 2309005, PR #5 / reservoir-adq.5). GoalsScreenCalculator.completionOutcome now has an explicit daysRemaining==0 + avgDailyNet\u003e0 case (returns .onSchedule) with a test. The avgDailyNet\u003c0 (behind pace) branch combined with daysRemaining==0 (goal due today, already behind) was not touched by that fix and remains untested — not a known regression, but an untested edge case worth locking down with a unit test asserting the late-completion date/day count is sane when the goal is due today.","status":"closed","priority":3,"issue_type":"task","owner":"johnjpease@gmail.com","created_at":"2026-07-10T02:59:43Z","created_by":"johnpease","updated_at":"2026-07-10T04:12:02Z","closed_at":"2026-07-10T04:12:02Z","close_reason":"Added testSimulationLateCompletionWhenTargetDateIsTodayAndBehindPace in commit 4c94b13, confirms the untouched .late branch stays correct at daysRemaining==0","dependency_count":0,"dependent_count":0,"comment_count":0}
{"id":"reservoir-yxl","title":"Cache CompletionBannerView.isGoalMet to avoid recompute on every body access","description":"TodayView.swift CompletionBannerView.isGoalMet (~line 360) is a computed property that re-runs the full carryForwardInput mapping (goal.transactions.map over all spend entries) plus DailyLimitCalculator's day-by-day carry-forward loop every time it's accessed. It's accessed at least twice per body render (icon selection at line 366, copy branch at line 369), so the full calculation runs twice per render.\n\nFlagged during code review of PR #4 (feature/goal-met-verification, reservoir-4za). Not currently impactful — single-user app, low transaction volume per goal — but worth fixing for hygiene/scalability. Fix: compute isGoalMet once (e.g. as a 'let' in body, or backing @State set in .onAppear/.task) and reuse the cached value for both the icon and copy branch, rather than re-deriving it on each access.","status":"open","priority":3,"issue_type":"task","owner":"johnjpease@gmail.com","created_at":"2026-07-09T01:04:46Z","created_by":"johnpease","updated_at":"2026-07-09T01:04:46Z","dependencies":[{"issue_id":"reservoir-yxl","depends_on_id":"reservoir-4za","type":"discovered-from","created_at":"2026-07-08T20:04:45Z","created_by":"johnpease","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
