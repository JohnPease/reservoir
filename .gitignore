
# Dolt database files (added by bd init)
.dolt/
*.db

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
.swiftpm/
*.xcscmblueprint

# Generated by `xcodegen generate` from project.yml — not committed, see README
*.xcodeproj

# Plaid credentials — Config/Plaid.xcconfig is committed with safe placeholder
# defaults so `xcodegen generate` always works on a fresh clone. Real
# credentials go in Config/Plaid.local.xcconfig (copy from
# Config/Plaid.local.xcconfig.example), which it #includes and which is never
# committed — see README.
Config/Plaid.local.xcconfig
